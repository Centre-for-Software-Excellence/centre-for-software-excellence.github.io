---
title: 'Write MDX Articles'
description: 'Article description from frontmatter (hidden from sidebar)'
date: '2025-06-23'
---

import { Divider } from '@/components/common/ui/divider';
import { Caution, Important, Note, Tip, Warning } from '@/components/md/alerts';

> file name of the `mdx/tsx` file will be used as the article title in the sidebar.
> file name starts with `_` will be hidden from the sidebar but still accessible via URL (e.g. [/docs/guide/\_write-MDX-articles]() and [/docs/guide/\_write-TSX-articles]()).

<Note>
  You can still use tsx components in mdx files. This is an example of a article
  written in MDX format, which allows you to use markdown syntax + React
  components. Frontmatter could be used to render a article main title. Table of
  contents is builtin for mdx files with at least one heading.
</Note>

# Header 1

## Header 2

### Header 3

#### Header 4

##### Header 5

###### Header 6

### Blockquote Content

> block content

### Inline Code Example

This is an `inline code example`

### Code Example

use syntax `{1-2, 3}` to highlight specific lines, `showLineNumbers` to show line numbers of the code block

```
def example_function():
    print("This is an example function.")
def another_function():
    print("This is another example function.")
```

```python {1-2, 3} showLineNumbers noheader
def example_function():
    print("This is an example function.")
def another_function():
    print("This is another example function.")
```

### List Example

- Item 1
- Item 2
- Item 3

### Table Example

| Header 1 | Header 2 |
| -------- | -------- |
| Row 1    | Data 1   |
| Row 2    | Data 2   |

### Link Example

[Example Link](https://example.com)

### Image Example

![Example Image](/logos/Huawei-logo.svg)

### Divider Example

<Divider hasToc={true} />

### Use react component in mdx

<Note>This is a note component rendered in MDX.</Note>
<Tip>This is a tip component rendered in MDX</Tip>
<Important>This is an important component rendered in MDX</Important>
<Caution>This is a caution component rendered in MDX</Caution>
<Warning>This is a warning component rendered in MDX</Warning>
